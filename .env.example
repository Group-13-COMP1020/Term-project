# VinRECIPE environment template
#
# The current application uses the local SQLite file `vinrecipe.db`.
# No environment variables are required for normal build/run.
# Keep this file as a placeholder for future optional configuration.
